# ─────────────────────────────────────────────────────────────
# Mobile App Configuration
# ─────────────────────────────────────────────────────────────
# Copy this file to .env for development:
#   cp .env.example .env
# ─────────────────────────────────────────────────────────────

# ─────────────────────────────────────────────────────────────
# Port Configuration
# ─────────────────────────────────────────────────────────────
# Ports are centrally defined in:
#   packages/utils/src/config.ts (SINGLE SOURCE OF TRUTH)
#   Default ports: API=3001, WEB=3000, DATABASE=5432, REDIS=6379
#
# Direct Mode (default): Each service on its own port
#   EXPO_PUBLIC_API_URL=http://localhost:3001
#
# Caddy Proxy Mode: All services behind https://localhost
#   EXPO_PUBLIC_API_URL=https://localhost
#   (Start with: pnpm dev:caddy)
#
# ⚠️ IMPORTANT for Physical Devices:
#   Use your computer's IP address instead of localhost
#   Example: EXPO_PUBLIC_API_URL=http://192.168.1.100:3001
#   Find your IP: ipconfig (Windows) or ifconfig (Mac/Linux)
# ─────────────────────────────────────────────────────────────

# API Server URL (required)
# This is used by the auth client to communicate with the API
# Use your local IP address (not localhost) for physical devices
EXPO_PUBLIC_API_URL=http://localhost:3001
