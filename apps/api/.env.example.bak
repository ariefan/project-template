# ─────────────────────────────────────────────────────────────
# Runtime Configuration for apps/api
# This is the ONLY source of truth for API server config
# ─────────────────────────────────────────────────────────────
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# ─────────────────────────────────────────────────────────────

# ─────────────────────────────────────────────────────────────
# 🔐 SECRETS (Keep these safe, never commit actual values!)
# ─────────────────────────────────────────────────────────────

# Auth (required)
BETTER_AUTH_SECRET=your-secret-key-minimum-32-characters-long

# Database (required)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/mydb

# OAuth Providers (optional)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Email API Keys
RESEND_API_KEY=

# SMTP Credentials (for nodemailer fallback)
SMTP_USER=
SMTP_PASS=

# Twilio Credentials (SMS + WhatsApp)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=

# Telegram Bot Token
TELEGRAM_BOT_TOKEN=

# Payment Gateway (optional)
# Log in to Xendit Dashboard → Settings → API Keys
XENDIT_SECRET_KEY=xnd_development_...
XENDIT_WEBHOOK_SECRET=your-webhook-verification-token

# ─────────────────────────────────────────────────────────────
# ⚙️ CONFIGURATION (Non-sensitive)
# ─────────────────────────────────────────────────────────────

# ─────────────────────────────────────────────────────────────
# Port Configuration
# ─────────────────────────────────────────────────────────────
# Ports are centrally defined in:
#   packages/utils/src/config.ts (SINGLE SOURCE OF TRUTH)
#   Default ports: API=3001, WEB=3000, DATABASE=5432, REDIS=6379
#
# Direct Mode (default): Each service on its own port
#   CORS_ORIGIN=http://localhost:3000
#   BETTER_AUTH_URL=http://localhost:3001
#
# Caddy Proxy Mode: All services behind https://localhost
#   CORS_ORIGIN=https://localhost
#   BETTER_AUTH_URL=https://localhost
#   (Start with: pnpm dev:caddy)
# ─────────────────────────────────────────────────────────────

# Environment
NODE_ENV=development
PORT=3001

# CORS
CORS_ORIGIN=http://localhost:3000

# Auth URLs
BETTER_AUTH_URL=http://localhost:3001

# Passkey / WebAuthn (optional)
PASSKEY_RP_ID=localhost
PASSKEY_RP_NAME=My App

# ─────────────────────────────────────────────────────────────
# 💾 Storage Configuration
# ─────────────────────────────────────────────────────────────
# Choose your storage provider: local | s3
#   local  - Stores files in local filesystem (default, development)
#   s3     - Stores files in S3 or S3-compatible service (production)
#
# For local storage:
#   STORAGE_PROVIDER=local
#   STORAGE_LOCAL_PATH=./uploads
#
# For S3 (AWS S3, Cloudflare R2, MinIO, etc.):
#   STORAGE_PROVIDER=s3
#   S3_ACCESS_KEY_ID=your-access-key
#   S3_SECRET_ACCESS_KEY=your-secret-key
#   S3_BUCKET=my-bucket
#   S3_REGION=us-east-1
#   S3_ENDPOINT=https://...  # Optional: for R2, MinIO, or other S3-compatible services
# ─────────────────────────────────────────────────────────────

STORAGE_PROVIDER=local
STORAGE_LOCAL_PATH=./uploads

# S3/R2 Credentials (required if STORAGE_PROVIDER=s3)
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=

# S3/R2 Configuration
S3_ENDPOINT=
S3_REGION=us-east-1
S3_BUCKET=my-bucket

# File Upload Limits
FILE_MAX_SIZE=52428800

# ─────────────────────────────────────────────────────────────
# 📧 Email Configuration
# ─────────────────────────────────────────────────────────----
# Provider: resend (recommended - free 100 emails/day)
#   Sign up at https://resend.com → Get API key → Add below
# Alternative: nodemailer (SMTP - requires SMTP_HOST, SMTP_PORT, etc.)
# Note: "console" provider available but not recommended (use In-App Only channel instead)
# ─────────────────────────────────────────────────────────────

EMAIL_PROVIDER=resend
EMAIL_FROM=noreply@example.com

# SMTP Configuration (for nodemailer fallback)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false

# ─────────────────────────────────────────────────────────────
# 📱 Notification Channels
# ─────────────────────────────────────────────────────────────

# Twilio Phone Numbers (SMS + WhatsApp)
TWILIO_SMS_FROM=
TWILIO_WHATSAPP_FROM=

# Notification Queue
QUEUE_ENABLED=false
QUEUE_CONCURRENCY=10
QUEUE_MAX_RETRIES=3

# ─────────────────────────────────────────────────────────────
# 🔐 Authorization & Rate Limiting
# ─────────────────────────────────────────────────────────────

# Authorization Cache
CACHE_PROVIDER=memory
CACHE_TTL=300
CACHE_MAX_SIZE=1000

# Redis Configuration (required if CACHE_PROVIDER=redis)
REDIS_URL=redis://localhost:6379
REDIS_KEY_PREFIX=authz:

# Rate Limiting (DDoS Protection) - Tier-Based
RATE_LIMIT_ENABLED=true
RATE_LIMIT_WINDOW="1 hour"

# Tier-based limits (requests per hour)
RATE_LIMIT_FREE=100
RATE_LIMIT_BASIC=1000
RATE_LIMIT_PRO=10000
RATE_LIMIT_ENTERPRISE=100000

# Idempotency
IDEMPOTENCY_ENABLED=true
IDEMPOTENCY_TTL=86400

# ─────────────────────────────────────────────────────────────
# 💳 Subscription Billing
# ─────────────────────────────────────────────────────────────
# Choose your active payment provider: xendit | stripe
# Default: xendit
# ─────────────────────────────────────────────────────────────

SUBSCRIPTION_PAYMENT_PROVIDER=xendit

# Xendit Environment: sandbox | production
XENDIT_ENVIRONMENT=sandbox

# ─────────────────────────────────────────────────────────────
# 📊 Metrics & Observability
# ─────────────────────────────────────────────────────────────

METRICS_ENABLED=true
METRICS_PREFIX=

# HTTP Caching
CACHING_ENABLED=true

# Deprecation Headers
DEPRECATION_ENABLED=true
