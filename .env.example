# ─────────────────────────────────────────────────────────────
# ⚠️  THIS FILE IS FOR LOCAL CLI TOOLS ONLY
# ⚠️  DO NOT ADD RUNTIME CONFIG HERE
# ─────────────────────────────────────────────────────────────
#
# This file is used by:
#   - drizzle-kit (migrations, generate, push)
#   - Other CLI tools that need database access
#
# Runtime services (apps/api, apps/web) read from their own .env files:
#   - API server: apps/api/.env (copy from apps/api/.env.example)
#   - Web app: apps/web/.env.local (copy from apps/web/.env.example)
#
# Port configuration is centrally defined in:
#   packages/utils/src/config.ts (SINGLE SOURCE OF TRUTH)
#   Default ports: API=3001, WEB=3000, DATABASE=5432, REDIS=6379
#
# ─────────────────────────────────────────────────────────────

# For drizzle-kit CLI (PostgreSQL default port: 5432)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/mydb
