# https://github.com/evilmartians/lefthook

pre-commit:
  parallel: true
  commands:
    biome:
      run: pnpm biome check --write --staged
      stage_fixed: true
    typecheck:
      run: pnpm typecheck

pre-push:
  commands:
    check:
      run: pnpm check
